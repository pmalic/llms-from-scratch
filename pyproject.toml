[project]
name = "llms-from-scratch"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
    "ipykernel",
    "ipywidgets",
    "tiktoken",
    "torch",
    "numpy",
    "matplotlib",
    "tensorflow",
    "tqdm",
    "pandas"
]
